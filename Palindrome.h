#pragma once
#include<iostream>
#include<string>

//Returns true if the string s is a palindrome
//@Param = the string, the starting point of the string, the end point of the string 
bool is_palindrome(std::string s, int b, int e);